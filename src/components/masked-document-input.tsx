"use client";
import type { ChangeEvent, InputHTMLAttributes } from "react";
function mask(value: string, type: "cpf" | "cnpj" | "phone") { const digits = value.replace(/\D/g, "").slice(0, type === "cpf" ? 11 : type === "cnpj" ? 14 : 11); if (type === "cpf") return digits.replace(/(\d{3})(\d)/, "$1.$2").replace(/(\d{3})(\d)/, "$1.$2").replace(/(\d{3})(\d{1,2})$/, "$1-$2"); if (type === "cnpj") return digits.replace(/(\d{2})(\d)/, "$1.$2").replace(/(\d{3})(\d)/, "$1.$2").replace(/(\d{3})(\d)/, "$1/$2").replace(/(\d{4})(\d{1,2})$/, "$1-$2"); return digits.length > 10 ? digits.replace(/(\d{2})(\d{5})(\d{1,4})/, "($1) $2-$3") : digits.replace(/(\d{2})(\d{4})(\d{1,4})/, "($1) $2-$3"); }
export function MaskedDocumentInput({ name = "cpf", type = "cpf", ...props }: { name?: string; type?: "cpf" | "cnpj" | "phone" } & Omit<InputHTMLAttributes<HTMLInputElement>, "name" | "type">) { function onChange(event: ChangeEvent<HTMLInputElement>) { event.currentTarget.value = mask(event.currentTarget.value, type); props.onChange?.(event); } return <input {...props} name={name} inputMode="numeric" type="text" onChange={onChange} placeholder={props.placeholder ?? (type === "cpf" ? "000.000.000-00" : type === "cnpj" ? "00.000.000/0000-00" : "(11) 98765-4321")} />; }
